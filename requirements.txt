# DDFS runtime dependencies. Only what the workstation runs.
# Legacy or one-off sets go in their own file: pip resolves across everything
# it is given, and on Meridian a tool nobody uses downgraded starlette for the
# tool in use.
#
# Install into a DDFS-only virtual environment. The workstation runs several
# tools on one host; a shared interpreter means one tool's install changes
# every other tool.
#
#   py -3.12 -m venv .venv
#   .venv\Scripts\python -m pip install -r requirements.txt
#   .venv\Scripts\python check_env.py

duckdb>=1.0
openpyxl>=3.1

# Everything else DDFS uses is standard library: http.server, socketserver,
# json, csv, base64, hmac, urllib, datetime, collections.
